interface:
  display_name: "Remote Claude Sessions"
  short_description: "Drive remote Claude Code over SSH sessions"
  default_prompt: "Use $remotecc-claude-session to run Claude Code in a remote SSH workspace with managed sessions."
